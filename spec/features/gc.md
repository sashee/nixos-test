## Auto GC

* after boot it deletes old generations and runs gc
* plus there is a weekly gc
* it does not run if the nixos-upgrade script is running

### Nix build directories

* nix build directories are cleaned up automatically

